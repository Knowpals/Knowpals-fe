# 知伴AI 教师端 — 设计答辩：后端相关问题准备

---

## 一、API 协议设计问题

### 1.1 GET 请求携带 JSON Body（非标准）

**现象**：`GET /api/v1/stat/class` 在接口文档中定义为 GET 方法但通过 Body 传递 `{ class_id, video_id }`。

**风险**：
- HTTP 规范中 GET 请求不应有 Body，部分代理/网关/浏览器会丢弃
- Axios 的 `request.get(url, { data })` 实际上不发送 Body（GET 方法的 data 被忽略）

**前端已采取的折中方案**：改为 query params 传递 `?class_id=...&video_id=...`

**答辩建议**：建议后端统一改为 `POST /stat/class` 或将参数放入 query string。如果必须保持 GET，需确认网关不会拦截 GET Body。

---

### 1.2 题型枚举值前后端不一致

**现象**：

| 层级 | 支持的类型 |
|------|-----------|
| 前端 UI | 单选题、多选题、判断题、填空题、简答题（5种） |
| 前端内部标识 | `single_choice`, `multiple_choice`, `true_false`, `fill_blank`, `short_answer` |
| 后端 `ReviewAddReq` | 仅 `choice \| fill \| judge`（3种） |
| 后端 `Question` 模型 | `string`（无约束，实际返回值未知） |

**问题**：
- 单选/多选都映射为 `choice`，类型信息丢失——后端无法区分单选题和多选题
- 简答题映射为 `subjective`，不在后端文档声明的枚举中，可能被拒绝
- 前后端对"题型"的建模粒度不一致

**答辩建议**：
- 统一前后端题型枚举，建议后端扩展为：`single_choice`, `multiple_choice`, `true_false`, `fill_blank`, `short_answer`
- 或至少在后端文档中明确所有接受的值及其语义

---

### 1.3 冗余字段

**现象**：`PUT /question/review/{question_id}` 的 Body 中要求 `questionID`（必填），但 URL Path 已有 `question_id`。

**风险**：前端传了不一致的值会导致难以排查的 Bug。

**答辩建议**：后端应从 Path 取 ID，Body 中的 `questionID` 改为可选（校验一致性用），或在文档中标注"必须与 Path 中的 question_id 一致"。

---

### 1.4 字段命名风格不一致

| 接口 | 字段示例 | 风格 |
|------|---------|------|
| `ReviewAddReq` | `time_ms`, `video_id` | snake_case |
| `ReviewUpdateReq` | `questionID`, `knowledge_pks` | camelCase / 混合 |
| `GetTaskProcessReq` | `jobID` | camelCase |
| `LoginResp` | `token` | camelCase |

**答辩建议**：全项目统一命名规范（建议统一为 snake_case 与数据库对齐，或 camelCase 与前端 JS 对齐）。

---

## 二、缺失的后端接口

### 2.1 前端被迫用假数据的页面

| 页面 | 假数据量 | 缺失的接口 |
|------|---------|-----------|
| **SemesterPortrait** | ~90 行硬编码 DEMO | 需 `GET /stat/semester/{class_id}` 返回学期级画像数据 |
| **StudentData** | 35 行 demoPersona（已改为真实数据，但只是基础统计） | 需 `GET /agent/teacher-persona/{student_id}` 返回学习风格、强项弱项、建议、学习路径 |
| **AnalysisVideoDetail** | 之前全页假数据（已修复，现在用真实视频列表和播放器） | — |

### 2.2 前端仅模拟操作的功能

| 功能 | 文件 | 缺失的接口 |
|------|------|-----------|
| 删除视频 | VideoManagement.jsx | `DELETE /video/{video_id}` |
| 删除班级学生 | ClassDetail.jsx | `DELETE /class/{class_id}/student/{student_id}` |
| 更新个人信息 | MyProfile.jsx | `PUT /user/update` 或 `PATCH /user/profile` |
| 视频列表筛选/分页 | 多个文件 | 当前列表接口无分页参数，数据量大时性能堪忧 |

### 2.3 教师端可用的学生端接口（跨角色复用问题）

学生端的以下接口教师端也应该能调用，但需确认权限：
- `GET /agent/persona` — 学生画像（教师查看某学生时应可用）
- `GET /agent/learning-analysis` — 学情分析
- `GET /agent/deep-practice` — 深度练习（教师可能需要预览）
- `GET /agent/small-kg` / `GET /agent/big-kg` — 知识图谱

**答辩建议**：梳理角色权限矩阵，明确哪些接口是"学生专属"、哪些是"教师可代为调用"。

---

## 三、安全相关问题

### 3.1 Token 存储方式

**当前方案**：JWT Token 存储在 `localStorage`，每次请求通过 Axios 拦截器附加 `Authorization: Bearer <token>`。

**潜在风险**：
- XSS 攻击可窃取 localStorage 中的 token
- 没有 refresh token 机制，token 过期后只能重新登录
- 401 响应时自动清除 token 跳转首页，但未做 CSRF 防护

**答辩建议**：
- 考虑使用 HttpOnly Cookie 存储 token（防 XSS）
- 增加 refresh token 机制
- 敏感操作（删除、发布）增加二次确认或验证码

### 3.2 权限控制

**当前方案**：前端通过 `AuthGuard` 组件读取 `localStorage.getItem('role')` 判断教师/学生角色。

**问题**：
- 角色信息存储在客户端，可被篡改
- 前端路由守卫是 UI 层面的，后端必须有对应鉴权
- 如果后端不校验角色，学生可通过 Postman 直接调用教师接口

**答辩建议**：确认后端每个接口都有角色校验，前端角色判断仅用于 UI 展示优化。

### 3.3 文件上传安全

**当前方案**：`POST /video/upload` 使用 multipart/form-data 上传。

**潜在风险**：
- 未限制文件大小（前端未做，后端是否有？）
- 未限制文件类型（仅文档说"支持 mp4"，实际校验在后端？）
- 大文件上传无断点续传，失败需重新上传

**答辩建议**：
- 后端应校验文件类型（白名单）和大小
- 考虑支持分片上传（chunked upload）+ 断点续传
- 上传进度可轮询 `video/task/process`，但目前只返回 `stage` 和 `status`，缺少百分比

---

## 四、数据模型问题

### 4.1 视频状态机

当前视频状态（从 `getMyUploadedVideos` 返回值推断）：`pending | reviewing | published | rejected`

**问题**：
- 状态转换规则不明确（pending → reviewing → published/rejected?）
- 删除操作如果后端不支持，已删除视频在前端的展示策略不明确
- 缺少 `draft` 状态（教师上传后但未提交审核）

### 4.2 题目与知识点的关联

`ReviewUpdateReq` 中有 `knowledge_pks: [integer]`，但 `ReviewAddReq` 中没有——添加题目时不能关联知识点，只能编辑时补充。这是设计如此还是遗漏？

### 4.3 班级-视频的多对多关系

- `publishVideo` 接受 `class_ids: [integer]`，支持一个视频同时发布到多个班级
- 但 `getVideoTasks(classId)` 返回该班级的所有视频
- 缺少"获取视频被发布到哪些班级"的反查接口

---

## 五、性能与可靠性

### 5.1 无分页

所有列表接口（班级列表、视频列表、学生列表）均无分页参数。当数据量大时：
- 响应体积过大
- 前端渲染性能下降
- 数据库查询压力大

**答辩建议**：增加 `page` / `page_size` 参数，或至少 `limit` / `offset`。

### 5.2 AI 接口超时

`agent/chat`、`agent/quiz`、`agent/report` 等 AI 接口可能耗时较长。前端 timeout 设置为 30 秒（`request.js`），大模型生成可能超时。

**答辩建议**：
- AI 接口考虑改为异步模式：提交任务 → 返回 job_id → 轮询结果
- `video/task/process` 已有 job 轮询机制，AI 接口可复用

### 5.3 并发与竞态

- 教师同时上传多个视频时，前端无队列控制
- 快速切换页面时，旧的 API 请求可能覆盖新数据（需 cancelToken 或 AbortController）

---

## 六、接口设计一致性

### 6.1 响应格式

目前统一为 `{ code: 0, data: ..., msg: "..." }`。

**问题**：部分接口 `data` 直接是对象（如 `LoginResp: { token }`），部分是嵌套对象（如 `GetClassInfoResp: { class_info: {...} }`）。嵌套层级不统一。

### 6.2 错误码

前端拦截器判断 `code === 0 || code === 200` 视为成功。但没有后端错误码文档，前端无法针对特定错误做差异化处理（如"邀请码无效" vs "已加入该班级"）。

**答辩建议**：定义业务错误码枚举，前端根据 code 做精细化提示。

---

## 七、建议的优先级排序

| 优先级 | 事项 | 影响 |
|--------|------|------|
| 🔴 P0 | Token 改为 HttpOnly Cookie + refresh token | 安全 |
| 🔴 P0 | 所有接口增加角色鉴权 | 安全 |
| 🟠 P1 | 新增 DELETE 视频/学生接口 | 功能完整性 |
| 🟠 P1 | 新增学期画像接口 | 核心功能（SemesterPortrait 全页假数据） |
| 🟠 P1 | 列表接口增加分页 | 性能 |
| 🟡 P2 | 统一字段命名规范 | 可维护性 |
| 🟡 P2 | 统一题型枚举值 | 数据一致性 |
| 🟡 P2 | AI 接口异步化 | 用户体验 |
| 🟢 P3 | 文件上传分片 + 进度百分比 | 体验优化 |
| 🟢 P3 | 统一 GET/POST 语义 | 规范 |

---

## 八、答辩话术建议

**当被问到"前端为什么有假数据"时**：
> 我们在开发过程中发现 SemesterPortrait、StudentData 等页面所需的学期级画像、学习风格分析等数据，后端尚未提供对应接口。为了不阻塞前端开发和 UI 验收，我们先用 Demo 数据搭建了完整页面，并通过注释和 TODO 标记了依赖的后端接口。后续只需后端提供接口，前端将 Demo 替换为 API 调用即可快速上线。

**当被问到"题型为什么有5种但后端只接受3种"时**：
> 这是前后端协议未对齐的问题。我们已经在 `questionTypes.js` 中建立了 `backendType` 映射层，前端5种题型在发送请求时自动转换为后端兼容的枚举值。建议后端后续扩展 `ReviewAddReq.type` 的枚举范围，与前端保持一致，这样可以保留"单选/多选"的区分信息。

**当被问到"安全性如何保证"时**：
> 当前依赖后端 JWT 鉴权 + 前端 Axios 拦截器自动附加 Token。但 localStorage 存在 XSS 风险，建议后续迁移至 HttpOnly Cookie。此外，前端路由守卫仅做 UI 拦截，真正的权限控制应在后端每个接口层面校验角色。

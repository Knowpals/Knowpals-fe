# 知伴AI 后端技术实现路径

---

## 整体架构建议

```
Nginx (反向代理 + 静态资源) 
  → Go/Gin 主服务 (业务 API)
  → Python/FastAPI 副服务 (AI 模块: agent/*)
  → PostgreSQL (主库) + Redis (缓存/队列)
  → MinIO/OSS (视频文件存储)
  → RabbitMQ (异步任务: 视频处理、AI 生成)
```

---

## 1. User 模块 — 认证与用户管理

### 技术栈：Go + JWT + Redis

```
POST /user/register        → 注册（邮箱+密码+验证码）
POST /user/loginByPassword  → 密码登录
POST /user/loginByCode      → 验证码登录
POST /user/sendCode         → 发送邮箱验证码
POST /user/forgotPassword   → 忘记密码
GET  /user/getUserInfo      → 获取当前用户信息
```

### 实现路径

**注册/登录**：
- 密码用 `bcrypt` 加盐哈希存储，不存明文
- 登录成功返回 JWT（Access Token 2h + Refresh Token 7d）
- Refresh Token 存 Redis `refresh:<user_id>` → 支持主动踢人/续期

**验证码**：
- 6位随机数字，存入 Redis `verify_code:<email>`，TTL 5分钟
- 发送间隔限制：同一 email 60s 内不可重复发送
- 使用 QQ邮箱 SMTP / 阿里云邮件推送 / SendGrid

**Token 策略**：
- 建议 Access Token 放在 HttpOnly Cookie（防 XSS），而非前端 `localStorage`
- 同时返回 `role`（teacher/student）字段，后续接口从 JWT claims 解析角色做鉴权

**鉴权中间件**：
```go
func AuthMiddleware(requiredRole string) gin.HandlerFunc {
    // 1. 从 Cookie/Header 提取 token
    // 2. 解析 JWT claims 获取 user_id + role
    // 3. 校验 role 是否匹配 requiredRole
    // 4. 将 user_id 注入 context
}
```

---

## 2. Class 模块 — 班级管理

### 技术栈：Go + PostgreSQL

```
POST /class/create          → 创建班级
GET  /class/info/{id}       → 班级详情
GET  /class/my-created      → 教师创建的班级列表
GET  /class/my-joined       → 学生加入的班级列表
GET  /class/students/{id}   → 班级学生列表
POST /class/join            → 学生加入班级
POST /class/quit/{id}       → 学生退出班级
```

### 实现路径

**数据模型**：
```sql
CREATE TABLE classes (
  id          SERIAL PRIMARY KEY,
  class_name  VARCHAR(255) NOT NULL,
  invite_code VARCHAR(8) UNIQUE NOT NULL,  -- 6-8位字母数字
  teacher_id  INT NOT NULL REFERENCES users(id),
  created_at  TIMESTAMP DEFAULT NOW()
);

CREATE TABLE class_members (
  class_id  INT REFERENCES classes(id),
  student_id INT REFERENCES users(id),
  joined_at TIMESTAMP DEFAULT NOW(),
  PRIMARY KEY (class_id, student_id)
);
```

**邀请码生成**：
- `crypto/rand` 生成 6 位字母数字混合码（如 `A3K9X2`）
- 建表时加 UNIQUE 约束，生成冲突则重试

**缺失的接口（建议补充）**：
- `DELETE /class/{id}/student/{student_id}` — 教师移除学生

---

## 3. Video 模块 — 视频上传与处理

### 技术栈：Go + FFmpeg + MinIO + RabbitMQ

```
POST /video/upload           → 上传视频
GET  /video/getDetail/{id}   → 视频详情（含分段+题目）
GET  /video/getTasks/{id}    → 班级视频任务列表
GET  /video/my-uploaded      → 教师上传的视频列表
POST /video/task/process     → 查询视频处理进度
```

### 实现路径

**上传流程**：
```
1. 前端 form-data 上传 → 后端接收文件
2. 存原始文件到 MinIO (videos/raw/{video_id}.mp4)
3. 写 DB: videos 表 (status=pending)
4. 投递 RabbitMQ 消息 → 异步消费者处理
5. 返回 job_id + video_id 给前端

消费者处理流水线:
  a. FFmpeg 转码 → H.264/AAC (兼容浏览器播放)
  b. FFmpeg 切片 → HLS (m3u8 + .ts 分段)
  c. 语音识别 → 生成字幕/分段文本 (Whisper/阿里云ASR)
  d. NLP 分析 → 提取知识点，生成视频分段
  e. 更新 DB: videos.status = 'processing' → 'completed'
  f. 存转码后文件到 MinIO (videos/hls/{video_id}/)
```

**轮询进度**：
- Redis 记录处理阶段 `video:job:{job_id}` → `{ stage, progress_pct }`
- 前端调 `POST /video/task/process` 轮询

**HLS 播放**：前端 `<video>` 标签直接用 m3u8 地址，无需等待完整下载。

**缺失的接口（建议补充）**：
- `DELETE /video/{video_id}` — 删除视频及关联文件

---

## 4. Question 模块 — 题目管理

### 技术栈：Go + PostgreSQL

```
POST   /question/review/add        → 添加题目
PUT    /question/review/{id}       → 修改题目
DELETE /question/review/{id}       → 删除题目
GET    /question/review/videos     → 待审核视频列表
GET    /question/generate/{id}     → AI生成习题（旧版）
POST   /question/answer            → 学生提交答案并自动批改
```

### 实现路径

**数据模型**：
```sql
CREATE TABLE questions (
  id          SERIAL PRIMARY KEY,
  video_id    INT NOT NULL REFERENCES videos(id),
  segment_id  INT REFERENCES segments(id),
  type        VARCHAR(20) NOT NULL,  -- single_choice|multiple_choice|true_false|fill_blank|short_answer
  content     TEXT NOT NULL,         -- 题目文本
  options     JSONB,                 -- ["A选项","B选项","C选项","D选项"]
  answer      JSONB NOT NULL,        -- 正确答案: "A" | ["A","C"] | "对" | "泰勒展开"
  analysis    TEXT,                  -- 解析
  time_ms     INT NOT NULL,          -- 视频时间戳（毫秒），用于定位分段
  difficulty  FLOAT DEFAULT 0.5,
  created_at  TIMESTAMP DEFAULT NOW()
);
```

**题型枚举（建议扩展为 5 种）**：
```
single_choice  → 单选题（answer 存 "A"）
multiple_choice → 多选题（answer 存 ["A","C"]）
true_false     → 判断题（answer 存 "对"/"错"）
fill_blank     → 填空题（answer 存 "答案1|答案2"）
short_answer   → 简答题（answer 存自由文本，AI 辅助批改）
```

**自动批改逻辑**（`POST /question/answer`）：
```go
// 1. 根据 question_id 查出正确答案
// 2. 按题型比对:
//    - single_choice: studentAnswer == correctAnswer
//    - multiple_choice: 数组排序后逐项匹配
//    - true_false: 归一化比对（"对/正确/true/T" → "对"）
//    - fill_blank: 模糊匹配（去空格、转小写）
//    - short_answer: 调用 AI 服务做语义相似度打分
// 3. 返回 { question_id, is_correct, answer, analysis }
```

**知识点关联**：`question_knowledge` 关联表
```sql
CREATE TABLE question_knowledge (
  question_id  INT REFERENCES questions(id),
  knowledge_id INT REFERENCES knowledge_points(id),
  PRIMARY KEY (question_id, knowledge_id)
);
```

---

## 5. Behavior 模块 — 学习行为追踪

### 技术栈：Go + PostgreSQL + Redis

```
POST /behavior/record               → 记录行为事件
POST /behavior/update-progress      → 更新观看进度
GET  /behavior/class-progress/{id}  → 班级视频观看进度
GET  /behavior/my/unfinished        → 学生未完成任务
```

### 实现路径

**行为事件模型**：
```sql
CREATE TABLE behavior_events (
  id         SERIAL PRIMARY KEY,
  user_id    INT NOT NULL,
  video_id   INT NOT NULL,
  segment_id INT,
  event      VARCHAR(20),  -- pause|replay|play|seek
  duration   INT,          -- 暂停/回看时长(秒)
  current_sec INT,         -- 当前播放位置
  created_at TIMESTAMP DEFAULT NOW()
);
```

**进度更新**：
- Redis 缓存 `progress:{user_id}:{video_id}` → `{ current_sec, updated_at }`
- 前端每隔 30s 上报一次当前播放位置
- 后台定时（每5分钟）将 Redis 数据批量写入 PostgreSQL
- 完成判断：`current_sec >= video.duration * 0.9` → 标记完成

**进度查询**：
```sql
-- 班级视频进度：JOIN class_members + behavior_events + videos
SELECT v.video_id, v.title, 
       COALESCE(MAX(b.current_sec), 0) AS watch_time,
       CASE WHEN MAX(b.current_sec) >= v.duration * 0.9 
            THEN 'finished' 
            WHEN v.deadline < NOW() THEN 'expired'
            ELSE 'todo' 
       END AS status
FROM videos v
LEFT JOIN behavior_events b ON v.id = b.video_id AND b.user_id = ?
WHERE v.id IN (SELECT video_id FROM class_video_tasks WHERE class_id = ?)
GROUP BY v.id;
```

---

## 6. Statistics 模块 — 统计分析

### 技术栈：Go + PostgreSQL（聚合查询 / 物化视图）

```
GET /stat/class?class_id=&video_id=   → 班级整体统计
GET /stat/student/{video_id}          → 学生单视频统计
GET /stat/student/overview            → 学生总体概览
```

### 实现路径

**班级统计**（`/stat/class`）：
```go
// 聚合查询，建议用物化视图提升性能
type ClassStat struct {
    Overview struct {
        AvgCorrectRate float64  // AVG(correct_rate) WHERE class_id=? AND video_id=?
        AvgTimeCost    int      // AVG(time_cost)
        CompleteRate   float64  // COUNT(finished) / COUNT(all)
        TotalPause     int      // SUM(pause_count)
    }
    WeakKnowledge  []KnowledgePoint  // GROUP BY knowledge_id, HAVING avg_mastery < threshold
    TopPauseAction []SegmentStat     // GROUP BY segment_id, ORDER BY pause_count DESC LIMIT N
    TopReplayAction []SegmentStat
    TopQuestions   []QuestionStat    // GROUP BY question_id, ORDER BY error_rate DESC
}
```

**学生统计**（`/stat/student/{video_id}`）：
```go
// 单用户 + 单视频维度的聚合
type StudentStat struct {
    CorrectRate    float64
    KnowledgePoints []struct {  // 每个知识点的掌握度
        KnowledgeID int
        Title       string
        MasterScore float64
    }
    PauseCount     int
    ReplayCount    int
    TimeCost       int
    Status         string  // finished|todo|expired
    TopPauseAction []SegmentStat
    TopReplayAction []SegmentStat
}
```

**性能优化**：
- 统计类查询数据量大时，用 PostgreSQL `MATERIALIZED VIEW` + 定时刷新（cron 每30分钟）
- 热点数据缓存 Redis，TTL 5分钟

---

## 7. Agent 模块 — AI 智能体

### 技术栈：Python FastAPI + LLM API + Redis

```
POST /agent/chat         → 智能对话
GET  /agent/history      → 聊天历史
POST /agent/quiz         → 生成习题
POST /agent/report       → 生成学情报告
GET  /agent/report       → 获取已存储报告
```

### 实现路径

**服务架构**：
```
Go 主服务 → HTTP/gRPC → Python AI 服务

Python 服务结构:
  ├── chat_service.py      # 对话生成
  ├── quiz_service.py      # 习题生成
  ├── report_service.py    # 报告生成
  ├── persona_service.py   # 学生画像
  ├── kg_service.py        # 知识图谱
  └── llm_client.py        # LLM API 封装
```

**对话（`/agent/chat`）**：
```python
# 1. 接收 { text, video_id?, knowledge_id? }
# 2. 从 PG 查上下文: 视频分段文本 + 知识点内容 + 历史对话(最近10轮)
# 3. 拼 Prompt → 调 LLM (GPT-4o / Claude / 本地模型)
# 4. 流式返回(SSE) 或 同步返回 reply
# 5. 存储到 chat_history 表
```

**习题生成（`/agent/quiz`）**：
```python
# Prompt: "根据视频'{title}'的内容，生成 {num_questions} 道{type}题，难度{difficulty}"
# 返回: [{ question, options, answer, analysis, type, difficulty, knowledge_id }]
# 教师确认后通过 /question/review/add 保存
```

**学情报告（`/agent/report`）**：
```python
# 输入: video_id + 学生行为数据(暂停/回放位置 + 答题正确率 + 耗时)
# LLM 分析:
#   - 薄弱知识点识别
#   - 学习行为模式分析
#   - 推荐复习片段
#   - 生成自然语言总结
# 输出: { items: [{ knowledge_id, mastery, summary, trend, weakness, recommended_segments }], overall_summary }
```

**异步化建议**：
```python
# 报告生成耗时长(可能10-30s)，建议改为异步:
# 1. POST /agent/report → 返回 { job_id }
# 2. Celery/RQ worker 后台生成 → 结果写 DB
# 3. GET /agent/report?video_id= → 前端轮询获取
```

---

## 8. Review/Publish 模块 — 审核发布

### 技术栈：Go + PostgreSQL + RabbitMQ

```
POST /video/review/publish    → 审核+发布+下发
```

### 实现路径

**发布流程**：
```go
func PublishVideo(videoID int, classIDs []int) error {
    tx := db.Begin()
    // 1. 校验: video.status == 'processing' (已生成题目但未发布)
    // 2. 更新 video.status = 'published'
    // 3. 为每个班级创建 video_task 记录
    for _, classID := range classIDs {
        tx.Create(&VideoTask{
            VideoID:  videoID,
            ClassID:  classID,
            Deadline: req.Deadline,  // 截止日期
            Status:   'todo',
        })
    }
    // 4. 可选: 发送通知给班级学生(WebSocket/App Push)
    tx.Commit()
    // 5. 投递消息队列: 异步计算班级统计数据
    mq.Publish("stat:refresh", classIDs)
}
```

---

## 9. 数据表总览

```sql
-- 核心业务表
users               (id, email, password_hash, username, role, created_at)
classes             (id, class_name, invite_code, teacher_id, created_at)
class_members       (class_id, student_id, joined_at)
class_video_tasks   (id, class_id, video_id, deadline, status, created_at)
videos              (id, title, url, duration, status, teacher_id, created_at)
segments            (id, video_id, start_ms, end_ms, text, segment_id)
questions           (id, video_id, segment_id, type, content, options, answer, analysis, time_ms, difficulty)
behavior_events     (id, user_id, video_id, segment_id, event, duration, current_sec, created_at)
chat_history        (id, user_id, video_id, knowledge_id, role, text, created_at)
knowledge_points    (id, title, content, parent_id)  -- 知识点树
question_knowledge  (question_id, knowledge_id)
video_jobs          (id, video_id, job_id, stage, status, progress_pct)
reports             (id, user_id, video_id, items_json, overall_summary, created_at)
```

---

## 10. 开发顺序建议

| 阶段 | 内容 | 预计工作量 |
|------|------|-----------|
| **Phase 1** | User 模块 + Class 模块 + 基础鉴权 | 1周 |
| **Phase 2** | Video 上传 + FFmpeg 转码管道 + MinIO | 1.5周 |
| **Phase 3** | Question 模块 + 自动批改 + 知识点关联 | 1周 |
| **Phase 4** | Behavior 追踪 + Statistics 统计聚合 | 1周 |
| **Phase 5** | Agent AI 服务 (对话+出题+报告) | 2周 |
| **Phase 6** | Review/Publish 发布流程 | 0.5周 |
| **Phase 7** | 补充缺失接口 (DELETE视频/学生、学期画像) | 1周 |
| **合计** | | ~8周 |
